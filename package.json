{
  "name": "my-elastique-quiz",
  "version": "1.0.0",
  "description": "starting both the client and server",
  "main": "server.js",
  "license": "MIT",
  "scripts": {
    "server:watch": "cd server && yarn watch",
    "client:watch": "cd app && yarn start",
    "watch": "concurrently \"yarn server:watch\" \"yarn client:watch\""
  },
  "devDependencies": {
    "concurrently": "^6.2.0",
    "npm-run-all": "^4.1.5"
  }
}
